{
  "name": "@worklog/rules-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project worklog-test \"vitest run\"",
    "test:watch": "firebase emulators:exec --only firestore,storage --project worklog-test \"vitest\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0",
    "firebase-tools": "^14.24.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
